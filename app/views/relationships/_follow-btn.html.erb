<% if current_user.following?(user) %>
    <%= link_to 'フォロー外す', unfollow_path(user.id), method: :POST, remote: true, class: 'btn btn-info w-auto' %>
<% else %>
    <%= link_to 'フォローする', follow_path(user.id), method: :POST, remote: true, class: 'btn btn-success w-auto' %>
<% end %>